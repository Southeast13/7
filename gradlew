#!/usr/bin/env sh
echo "This is a stub gradlew script included for repository completeness."
echo "If you want a working Gradle wrapper locally, run: gradle wrapper"
echo "On Codemagic we install Gradle in the CI instead."
exec gradle "$@"
